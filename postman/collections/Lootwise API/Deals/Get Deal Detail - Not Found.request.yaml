$kind: http-request
description: Not found example.
url: "{{baseUrl}}/api/v1/deals/999999999"
method: GET
headers:
  Accept: application/json
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 404", function () {
          pm.response.to.have.status(404);
      });
    language: text/javascript
order: 4000
